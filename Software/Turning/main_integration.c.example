/* Superseded by the real Task 1 integration in this folder:
 *   main.c          PSoC startup and 5 ms scheduler
 *   main_control.c  line following + automatic corner detection + turn handoff
 *   main_control.h  MainControlStart/Update5ms and status APIs
 *
 * Use these files instead of copying the old route-based example.
 * No GetPlannedTurn/FollowLineUpdate/HandleTurnComplete placeholders remain.
 * See README.md for PSoC Creator build instructions.
 */
#include "main_control.h"
